var app = (function () {
'use strict';

function noop() {}

function assign(tar, src) {
	for (var k in src) tar[k] = src[k];
	return tar;
}

function addLoc(element, file, line, column, char) {
	element.__svelte_meta = {
		loc: { file, line, column, char }
	};
}

function appendNode(node, target) {
	target.appendChild(node);
}

function insertNode(node, target, anchor) {
	target.insertBefore(node, anchor);
}

function detachNode(node) {
	node.parentNode.removeChild(node);
}

function destroyEach(iterations, detach) {
	for (var i = 0; i < iterations.length; i += 1) {
		if (iterations[i]) iterations[i].d(detach);
	}
}

function createSvgElement(name) {
	return document.createElementNS('http://www.w3.org/2000/svg', name);
}

function createText(data) {
	return document.createTextNode(data);
}

function createComment() {
	return document.createComment('');
}

function setAttribute(node, attribute, value) {
	node.setAttribute(attribute, value);
}

function setXlinkAttribute(node, attribute, value) {
	node.setAttributeNS('http://www.w3.org/1999/xlink', attribute, value);
}

function blankObject() {
	return Object.create(null);
}

function destroy(detach) {
	this.destroy = noop;
	this.fire('destroy');
	this.set = noop;

	this._fragment.d(detach !== false);
	this._fragment = null;
	this._state = {};
}

function destroyDev(detach) {
	destroy.call(this, detach);
	this.destroy = function() {
		console.warn('Component was already destroyed');
	};
}

function _differs(a, b) {
	return a != a ? b == b : a !== b || ((a && typeof a === 'object') || typeof a === 'function');
}

function fire(eventName, data) {
	var handlers =
		eventName in this._handlers && this._handlers[eventName].slice();
	if (!handlers) return;

	for (var i = 0; i < handlers.length; i += 1) {
		var handler = handlers[i];

		if (!handler.__calling) {
			handler.__calling = true;
			handler.call(this, data);
			handler.__calling = false;
		}
	}
}

function get() {
	return this._state;
}

function init(component, options) {
	component._handlers = blankObject();
	component._bind = options._bind;

	component.options = options;
	component.root = options.root || component;
	component.store = component.root.store || options.store;
}

function on(eventName, handler) {
	var handlers = this._handlers[eventName] || (this._handlers[eventName] = []);
	handlers.push(handler);

	return {
		cancel: function() {
			var index = handlers.indexOf(handler);
			if (~index) handlers.splice(index, 1);
		}
	};
}

function set(newState) {
	this._set(assign({}, newState));
	if (this.root._lock) return;
	this.root._lock = true;
	callAll(this.root._beforecreate);
	callAll(this.root._oncreate);
	callAll(this.root._aftercreate);
	this.root._lock = false;
}

function _set(newState) {
	var oldState = this._state,
		changed = {},
		dirty = false;

	for (var key in newState) {
		if (this._differs(newState[key], oldState[key])) changed[key] = dirty = true;
	}
	if (!dirty) return;

	this._state = assign(assign({}, oldState), newState);
	this._recompute(changed, this._state);
	if (this._bind) this._bind(changed, this._state);

	if (this._fragment) {
		this.fire("state", { changed: changed, current: this._state, previous: oldState });
		this._fragment.p(changed, this._state);
		this.fire("update", { changed: changed, current: this._state, previous: oldState });
	}
}

function setDev(newState) {
	if (typeof newState !== 'object') {
		throw new Error(
			this._debugName + '.set was called without an object of data key-values to update.'
		);
	}

	this._checkReadOnly(newState);
	set.call(this, newState);
}

function callAll(fns) {
	while (fns && fns.length) fns.shift()();
}

function _mount(target, anchor) {
	this._fragment[this._fragment.i ? 'i' : 'm'](target, anchor || null);
}

var protoDev = {
	destroy: destroyDev,
	get,
	fire,
	on,
	set: setDev,
	_recompute: noop,
	_set,
	_mount,
	_differs
};

/* src/Arc.html generated by Svelte v2.7.0 */

function p1x({innerRadius, centerX, startRadians}) {
	return centerX + (innerRadius * Math.sin(startRadians));
}

function p1y({innerRadius, centerY, startRadians}) {
	return centerY - (innerRadius * Math.cos(startRadians));
}

function p2x({innerRadius, centerX, endRadians}) {
	return centerX + (innerRadius * Math.sin(endRadians));
}

function p2y({innerRadius, centerY, endRadians}) {
	return centerY - (innerRadius * Math.cos(endRadians));
}

function p3x({outerRadius, centerX, endRadians}) {
	return centerX + (outerRadius * Math.sin(endRadians));
}

function p3y({outerRadius, centerY, endRadians}) {
	return centerY - (outerRadius * Math.cos(endRadians));
}

function p4x({outerRadius, centerX, startRadians}) {
	return centerX + (outerRadius * Math.sin(startRadians));
}

function p4y({outerRadius, centerY, startRadians}) {
	return centerY - (outerRadius * Math.cos(startRadians));
}

function arcSweep({endRadians, startRadians}) {
	return (endRadians - startRadians <= Math.PI) ? "0" : "1";
}

function pMRadius({innerRadius, outerRadius}) {
	return (innerRadius + outerRadius) / 2;
}

function pMStartX({pMRadius, centerX, startRadians}) {
	return centerX + (pMRadius * Math.sin(startRadians));
}

function pMStartY({pMRadius, centerY, startRadians}) {
	return centerY - (pMRadius * Math.cos(startRadians));
}

function pMEndX({pMRadius, centerX, endRadians}) {
	return centerX + (pMRadius * Math.sin(endRadians));
}

function pMEndY({pMRadius, centerY, endRadians}) {
	return centerY - (pMRadius * Math.cos(endRadians));
}

function data() {
  return {
    id: 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
        var r = crypto.getRandomValues(new Uint8Array(1))[0]%16|0, v = c == 'x' ? r : (r&0x3|0x8);
        return v.toString(16)
    }),
    fontSize: 3,
    fontColor: "black",
    strokeWidth: .3,
    stroke: "#333333",
    fill: "orange",
    opacity: 1,
    innerRadius: 10,
    outerRadius: 43,
    centerX: 50,
    centerY: 50,
    startRadians: 0,  // 0 is top dead center unlike the math convention
    endRadians: Math.PI / 4,  // Clockwise from startRadians
    label: "",
  }
}
const file = "src/Arc.html";

function create_main_fragment(component, ctx) {
	var svg, defs, path, path_d_value, path_1, path_1_d_value, text, textPath, text_1, textPath_xlink_href_value;

	return {
		c: function create() {
			svg = createSvgElement("svg");
			defs = createSvgElement("defs");
			path = createSvgElement("path");
			path_1 = createSvgElement("path");
			text = createSvgElement("text");
			textPath = createSvgElement("textPath");
			text_1 = createText(ctx.label);
			setAttribute(path, "id", ctx.id);
			setAttribute(path, "d", path_d_value = "\n        M " + ctx.pMStartX + " " + ctx.pMStartY + "\n        A " + ctx.pMRadius + " " + ctx.pMRadius + " 0 " + ctx.arcSweep + " 1 " + ctx.pMEndX + " " + ctx.pMEndY + "\n      ");
			addLoc(path, file, 2, 4, 19);
			addLoc(defs, file, 1, 2, 8);
			setAttribute(path_1, "fill", ctx.fill);
			setAttribute(path_1, "opacity", ctx.opacity);
			setAttribute(path_1, "stroke", ctx.stroke);
			setAttribute(path_1, "stroke-width", ctx.strokeWidth);
			setAttribute(path_1, "stroke-linecap", "square");
			setAttribute(path_1, "d", path_1_d_value = "\n      M " + ctx.p1x + " " + ctx.p1y + "\n      A " + ctx.innerRadius + " " + ctx.innerRadius + " 0 " + ctx.arcSweep + " 1 " + ctx.p2x + " " + ctx.p2y + "\n      L " + ctx.p3x + " " + ctx.p3y + "\n      A " + ctx.outerRadius + " " + ctx.outerRadius + " 1 " + ctx.arcSweep + " 0 " + ctx.p4x + " " + ctx.p4y + "\n      L " + ctx.p1x + " " + ctx.p1y + "\n    ");
			addLoc(path_1, file, 10, 2, 173);
			setXlinkAttribute(textPath, "xlink:href", textPath_xlink_href_value = "#" + ctx.id);
			setAttribute(textPath, "alignment-baseline", "middle");
			setAttribute(textPath, "text-anchor", "middle");
			setAttribute(textPath, "startOffset", "50%");
			setAttribute(textPath, "fill", ctx.fontColor);
			addLoc(textPath, file, 21, 4, 519);
			setAttribute(text, "font-size", ctx.fontSize);
			addLoc(text, file, 20, 2, 487);
			addLoc(svg, file, 0, 0, 0);
		},

		m: function mount(target, anchor) {
			insertNode(svg, target, anchor);
			appendNode(defs, svg);
			appendNode(path, defs);
			appendNode(path_1, svg);
			appendNode(text, svg);
			appendNode(textPath, text);
			appendNode(text_1, textPath);
		},

		p: function update(changed, ctx) {
			if (changed.id) {
				setAttribute(path, "id", ctx.id);
			}

			if ((changed.pMStartX || changed.pMStartY || changed.pMRadius || changed.arcSweep || changed.pMEndX || changed.pMEndY) && path_d_value !== (path_d_value = "\n        M " + ctx.pMStartX + " " + ctx.pMStartY + "\n        A " + ctx.pMRadius + " " + ctx.pMRadius + " 0 " + ctx.arcSweep + " 1 " + ctx.pMEndX + " " + ctx.pMEndY + "\n      ")) {
				setAttribute(path, "d", path_d_value);
			}

			if (changed.fill) {
				setAttribute(path_1, "fill", ctx.fill);
			}

			if (changed.opacity) {
				setAttribute(path_1, "opacity", ctx.opacity);
			}

			if (changed.stroke) {
				setAttribute(path_1, "stroke", ctx.stroke);
			}

			if (changed.strokeWidth) {
				setAttribute(path_1, "stroke-width", ctx.strokeWidth);
			}

			if ((changed.p1x || changed.p1y || changed.innerRadius || changed.arcSweep || changed.p2x || changed.p2y || changed.p3x || changed.p3y || changed.outerRadius || changed.p4x || changed.p4y) && path_1_d_value !== (path_1_d_value = "\n      M " + ctx.p1x + " " + ctx.p1y + "\n      A " + ctx.innerRadius + " " + ctx.innerRadius + " 0 " + ctx.arcSweep + " 1 " + ctx.p2x + " " + ctx.p2y + "\n      L " + ctx.p3x + " " + ctx.p3y + "\n      A " + ctx.outerRadius + " " + ctx.outerRadius + " 1 " + ctx.arcSweep + " 0 " + ctx.p4x + " " + ctx.p4y + "\n      L " + ctx.p1x + " " + ctx.p1y + "\n    ")) {
				setAttribute(path_1, "d", path_1_d_value);
			}

			if (changed.label) {
				text_1.data = ctx.label;
			}

			if ((changed.id) && textPath_xlink_href_value !== (textPath_xlink_href_value = "#" + ctx.id)) {
				setXlinkAttribute(textPath, "xlink:href", textPath_xlink_href_value);
			}

			if (changed.fontColor) {
				setAttribute(textPath, "fill", ctx.fontColor);
			}

			if (changed.fontSize) {
				setAttribute(text, "font-size", ctx.fontSize);
			}
		},

		d: function destroy$$1(detach) {
			if (detach) {
				detachNode(svg);
			}
		}
	};
}

function Arc(options) {
	this._debugName = '<Arc>';
	if (!options || (!options.target && !options.root)) throw new Error("'target' is a required option");
	init(this, options);
	this._state = assign(data(), options.data);
	this._recompute({ innerRadius: 1, centerX: 1, startRadians: 1, centerY: 1, endRadians: 1, outerRadius: 1, pMRadius: 1 }, this._state);
	if (!('innerRadius' in this._state)) console.warn("<Arc> was created without expected data property 'innerRadius'");
	if (!('centerX' in this._state)) console.warn("<Arc> was created without expected data property 'centerX'");
	if (!('startRadians' in this._state)) console.warn("<Arc> was created without expected data property 'startRadians'");
	if (!('centerY' in this._state)) console.warn("<Arc> was created without expected data property 'centerY'");
	if (!('endRadians' in this._state)) console.warn("<Arc> was created without expected data property 'endRadians'");
	if (!('outerRadius' in this._state)) console.warn("<Arc> was created without expected data property 'outerRadius'");

	if (!('id' in this._state)) console.warn("<Arc> was created without expected data property 'id'");





	if (!('fill' in this._state)) console.warn("<Arc> was created without expected data property 'fill'");
	if (!('opacity' in this._state)) console.warn("<Arc> was created without expected data property 'opacity'");
	if (!('stroke' in this._state)) console.warn("<Arc> was created without expected data property 'stroke'");
	if (!('strokeWidth' in this._state)) console.warn("<Arc> was created without expected data property 'strokeWidth'");








	if (!('fontSize' in this._state)) console.warn("<Arc> was created without expected data property 'fontSize'");
	if (!('fontColor' in this._state)) console.warn("<Arc> was created without expected data property 'fontColor'");
	if (!('label' in this._state)) console.warn("<Arc> was created without expected data property 'label'");
	this._intro = true;

	this._fragment = create_main_fragment(this, this._state);

	if (options.target) {
		if (options.hydrate) throw new Error("options.hydrate only works if the component was compiled with the `hydratable: true` option");
		this._fragment.c();
		this._mount(options.target, options.anchor);
	}
}

assign(Arc.prototype, protoDev);

Arc.prototype._checkReadOnly = function _checkReadOnly(newState) {
	if ('p1x' in newState && !this._updatingReadonlyProperty) throw new Error("<Arc>: Cannot set read-only property 'p1x'");
	if ('p1y' in newState && !this._updatingReadonlyProperty) throw new Error("<Arc>: Cannot set read-only property 'p1y'");
	if ('p2x' in newState && !this._updatingReadonlyProperty) throw new Error("<Arc>: Cannot set read-only property 'p2x'");
	if ('p2y' in newState && !this._updatingReadonlyProperty) throw new Error("<Arc>: Cannot set read-only property 'p2y'");
	if ('p3x' in newState && !this._updatingReadonlyProperty) throw new Error("<Arc>: Cannot set read-only property 'p3x'");
	if ('p3y' in newState && !this._updatingReadonlyProperty) throw new Error("<Arc>: Cannot set read-only property 'p3y'");
	if ('p4x' in newState && !this._updatingReadonlyProperty) throw new Error("<Arc>: Cannot set read-only property 'p4x'");
	if ('p4y' in newState && !this._updatingReadonlyProperty) throw new Error("<Arc>: Cannot set read-only property 'p4y'");
	if ('arcSweep' in newState && !this._updatingReadonlyProperty) throw new Error("<Arc>: Cannot set read-only property 'arcSweep'");
	if ('pMRadius' in newState && !this._updatingReadonlyProperty) throw new Error("<Arc>: Cannot set read-only property 'pMRadius'");
	if ('pMStartX' in newState && !this._updatingReadonlyProperty) throw new Error("<Arc>: Cannot set read-only property 'pMStartX'");
	if ('pMStartY' in newState && !this._updatingReadonlyProperty) throw new Error("<Arc>: Cannot set read-only property 'pMStartY'");
	if ('pMEndX' in newState && !this._updatingReadonlyProperty) throw new Error("<Arc>: Cannot set read-only property 'pMEndX'");
	if ('pMEndY' in newState && !this._updatingReadonlyProperty) throw new Error("<Arc>: Cannot set read-only property 'pMEndY'");
};

Arc.prototype._recompute = function _recompute(changed, state) {
	if (changed.innerRadius || changed.centerX || changed.startRadians) {
		if (this._differs(state.p1x, (state.p1x = p1x(state)))) changed.p1x = true;
	}

	if (changed.innerRadius || changed.centerY || changed.startRadians) {
		if (this._differs(state.p1y, (state.p1y = p1y(state)))) changed.p1y = true;
	}

	if (changed.innerRadius || changed.centerX || changed.endRadians) {
		if (this._differs(state.p2x, (state.p2x = p2x(state)))) changed.p2x = true;
	}

	if (changed.innerRadius || changed.centerY || changed.endRadians) {
		if (this._differs(state.p2y, (state.p2y = p2y(state)))) changed.p2y = true;
	}

	if (changed.outerRadius || changed.centerX || changed.endRadians) {
		if (this._differs(state.p3x, (state.p3x = p3x(state)))) changed.p3x = true;
	}

	if (changed.outerRadius || changed.centerY || changed.endRadians) {
		if (this._differs(state.p3y, (state.p3y = p3y(state)))) changed.p3y = true;
	}

	if (changed.outerRadius || changed.centerX || changed.startRadians) {
		if (this._differs(state.p4x, (state.p4x = p4x(state)))) changed.p4x = true;
	}

	if (changed.outerRadius || changed.centerY || changed.startRadians) {
		if (this._differs(state.p4y, (state.p4y = p4y(state)))) changed.p4y = true;
	}

	if (changed.endRadians || changed.startRadians) {
		if (this._differs(state.arcSweep, (state.arcSweep = arcSweep(state)))) changed.arcSweep = true;
	}

	if (changed.innerRadius || changed.outerRadius) {
		if (this._differs(state.pMRadius, (state.pMRadius = pMRadius(state)))) changed.pMRadius = true;
	}

	if (changed.pMRadius || changed.centerX || changed.startRadians) {
		if (this._differs(state.pMStartX, (state.pMStartX = pMStartX(state)))) changed.pMStartX = true;
	}

	if (changed.pMRadius || changed.centerY || changed.startRadians) {
		if (this._differs(state.pMStartY, (state.pMStartY = pMStartY(state)))) changed.pMStartY = true;
	}

	if (changed.pMRadius || changed.centerX || changed.endRadians) {
		if (this._differs(state.pMEndX, (state.pMEndX = pMEndX(state)))) changed.pMEndX = true;
	}

	if (changed.pMRadius || changed.centerY || changed.endRadians) {
		if (this._differs(state.pMEndY, (state.pMEndY = pMEndY(state)))) changed.pMEndY = true;
	}
};

/* src/Slice.html generated by Svelte v2.7.0 */

function dataOuterRadius({outerRadius, labelBandHeight}) {
	return outerRadius - labelBandHeight;
}

function levelsAnnotated({levels, innerRadius, dataOuterRadius}) {
  let levelsAnnotated = [];
  let total = 0;
  let count = 0;
  for (let level of levels) {
    count++;
    total += level.portion;
  }
  let dataRangeRadius = dataOuterRadius - innerRadius;
  let lastAnnotatedLevel = null;
  let i = 0;
  for (let level of levels) {
    let currentAnnotatedLevel = {};
    if (lastAnnotatedLevel) {
      currentAnnotatedLevel.innerRadius = lastAnnotatedLevel.outerRadius;
      currentAnnotatedLevel.opacity = (count - i - 1) / (count - 1);
    } else {
      currentAnnotatedLevel.innerRadius = innerRadius;
      currentAnnotatedLevel.opacity = 1;
    }
    currentAnnotatedLevel.outerRadius = currentAnnotatedLevel.innerRadius + dataRangeRadius * (level.portion / total);
    levelsAnnotated.push(currentAnnotatedLevel);
    lastAnnotatedLevel = currentAnnotatedLevel;
    i++;
  }
  return levelsAnnotated
}

function data$1() {
  return {
    strokeWidth: 0,
    stroke: "#999999",
    strokeWidth: 0.15,
    baseColor: "#2E8468",
    fontColor: "#22644E",
    innerRadius: 10,
    outerRadius: 43,
    startRadians: 0,  // 0 is top dead center unlike the math convention
    endRadians: Math.PI / 4,  // Clockwise from startRadians
    label: "Label",
    labelBandHeight: 5,
    levels: [
      {portion: 4},
      {portion: 2},
      {portion: 2},
      {portion: 1},
      {portion: 1},
    ]
  }
}
const file$1 = "src/Slice.html";

function create_main_fragment$1(component, ctx) {
	var svg, each_anchor;

	var arc_initial_data = {
	 	startRadians: ctx.startRadians,
	 	endRadians: ctx.endRadians,
	 	outerRadius: ctx.outerRadius,
	 	innerRadius: ctx.dataOuterRadius,
	 	label: ctx.label,
	 	fontColor: ctx.fontColor,
	 	strokeWidth: ctx.strokeWidth,
	 	fill: "none",
	 	stroke: ctx.stroke
	 };
	var arc = new Arc({
		root: component.root,
		data: arc_initial_data
	});

	var each_value = ctx.levelsAnnotated;

	var each_blocks = [];

	for (var i = 0; i < each_value.length; i += 1) {
		each_blocks[i] = create_each_block(component, get_each_context(ctx, each_value, i));
	}

	var arc_1_initial_data = {
	 	startRadians: ctx.startRadians,
	 	endRadians: ctx.endRadians,
	 	outerRadius: ctx.dataOuterRadius,
	 	innerRadius: ctx.innerRadius,
	 	strokeWidth: ctx.strokeWidth,
	 	fill: "none",
	 	stroke: ctx.stroke
	 };
	var arc_1 = new Arc({
		root: component.root,
		data: arc_1_initial_data
	});

	return {
		c: function create() {
			svg = createSvgElement("svg");
			arc._fragment.c();

			for (var i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}

			each_anchor = createComment();
			arc_1._fragment.c();
			addLoc(svg, file$1, 0, 0, 0);
		},

		m: function mount(target, anchor) {
			insertNode(svg, target, anchor);
			arc._mount(svg, null);

			for (var i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].m(svg, null);
			}

			appendNode(each_anchor, svg);
			arc_1._mount(svg, null);
		},

		p: function update(changed, ctx) {
			var arc_changes = {};
			if (changed.startRadians) arc_changes.startRadians = ctx.startRadians;
			if (changed.endRadians) arc_changes.endRadians = ctx.endRadians;
			if (changed.outerRadius) arc_changes.outerRadius = ctx.outerRadius;
			if (changed.dataOuterRadius) arc_changes.innerRadius = ctx.dataOuterRadius;
			if (changed.label) arc_changes.label = ctx.label;
			if (changed.fontColor) arc_changes.fontColor = ctx.fontColor;
			if (changed.strokeWidth) arc_changes.strokeWidth = ctx.strokeWidth;
			if (changed.stroke) arc_changes.stroke = ctx.stroke;
			arc._set(arc_changes);

			if (changed.startRadians || changed.endRadians || changed.levelsAnnotated || changed.baseColor) {
				each_value = ctx.levelsAnnotated;

				for (var i = 0; i < each_value.length; i += 1) {
					const child_ctx = get_each_context(ctx, each_value, i);

					if (each_blocks[i]) {
						each_blocks[i].p(changed, child_ctx);
					} else {
						each_blocks[i] = create_each_block(component, child_ctx);
						each_blocks[i].c();
						each_blocks[i].m(svg, each_anchor);
					}
				}

				for (; i < each_blocks.length; i += 1) {
					each_blocks[i].d(1);
				}
				each_blocks.length = each_value.length;
			}

			var arc_1_changes = {};
			if (changed.startRadians) arc_1_changes.startRadians = ctx.startRadians;
			if (changed.endRadians) arc_1_changes.endRadians = ctx.endRadians;
			if (changed.dataOuterRadius) arc_1_changes.outerRadius = ctx.dataOuterRadius;
			if (changed.innerRadius) arc_1_changes.innerRadius = ctx.innerRadius;
			if (changed.strokeWidth) arc_1_changes.strokeWidth = ctx.strokeWidth;
			if (changed.stroke) arc_1_changes.stroke = ctx.stroke;
			arc_1._set(arc_1_changes);
		},

		d: function destroy$$1(detach) {
			if (detach) {
				detachNode(svg);
			}

			arc.destroy();

			destroyEach(each_blocks, detach);

			arc_1.destroy();
		}
	};
}

// (6:2) {#each levelsAnnotated as level}
function create_each_block(component, ctx) {

	var arc_initial_data = {
	 	startRadians: ctx.startRadians,
	 	endRadians: ctx.endRadians,
	 	innerRadius: ctx.level.innerRadius,
	 	outerRadius: ctx.level.outerRadius,
	 	fill: ctx.baseColor,
	 	opacity: ctx.level.opacity,
	 	stroke: "none"
	 };
	var arc = new Arc({
		root: component.root,
		data: arc_initial_data
	});

	return {
		c: function create() {
			arc._fragment.c();
		},

		m: function mount(target, anchor) {
			arc._mount(target, anchor);
		},

		p: function update(changed, ctx) {
			var arc_changes = {};
			if (changed.startRadians) arc_changes.startRadians = ctx.startRadians;
			if (changed.endRadians) arc_changes.endRadians = ctx.endRadians;
			if (changed.levelsAnnotated) arc_changes.innerRadius = ctx.level.innerRadius;
			if (changed.levelsAnnotated) arc_changes.outerRadius = ctx.level.outerRadius;
			if (changed.baseColor) arc_changes.fill = ctx.baseColor;
			if (changed.levelsAnnotated) arc_changes.opacity = ctx.level.opacity;
			arc._set(arc_changes);
		},

		d: function destroy$$1(detach) {
			arc.destroy(detach);
		}
	};
}

function get_each_context(ctx, list, i) {
	const child_ctx = Object.create(ctx);
	child_ctx.level = list[i];
	child_ctx.each_value = list;
	child_ctx.level_index = i;
	return child_ctx;
}

function Slice(options) {
	this._debugName = '<Slice>';
	if (!options || (!options.target && !options.root)) throw new Error("'target' is a required option");
	init(this, options);
	this._state = assign(data$1(), options.data);
	this._recompute({ outerRadius: 1, labelBandHeight: 1, levels: 1, innerRadius: 1, dataOuterRadius: 1 }, this._state);
	if (!('outerRadius' in this._state)) console.warn("<Slice> was created without expected data property 'outerRadius'");
	if (!('labelBandHeight' in this._state)) console.warn("<Slice> was created without expected data property 'labelBandHeight'");
	if (!('levels' in this._state)) console.warn("<Slice> was created without expected data property 'levels'");
	if (!('innerRadius' in this._state)) console.warn("<Slice> was created without expected data property 'innerRadius'");

	if (!('startRadians' in this._state)) console.warn("<Slice> was created without expected data property 'startRadians'");
	if (!('endRadians' in this._state)) console.warn("<Slice> was created without expected data property 'endRadians'");
	if (!('label' in this._state)) console.warn("<Slice> was created without expected data property 'label'");
	if (!('fontColor' in this._state)) console.warn("<Slice> was created without expected data property 'fontColor'");
	if (!('strokeWidth' in this._state)) console.warn("<Slice> was created without expected data property 'strokeWidth'");
	if (!('stroke' in this._state)) console.warn("<Slice> was created without expected data property 'stroke'");

	if (!('baseColor' in this._state)) console.warn("<Slice> was created without expected data property 'baseColor'");
	this._intro = true;

	if (!options.root) {
		this._oncreate = [];
		this._beforecreate = [];
		this._aftercreate = [];
	}

	this._fragment = create_main_fragment$1(this, this._state);

	if (options.target) {
		if (options.hydrate) throw new Error("options.hydrate only works if the component was compiled with the `hydratable: true` option");
		this._fragment.c();
		this._mount(options.target, options.anchor);

		this._lock = true;
		callAll(this._beforecreate);
		callAll(this._oncreate);
		callAll(this._aftercreate);
		this._lock = false;
	}
}

assign(Slice.prototype, protoDev);

Slice.prototype._checkReadOnly = function _checkReadOnly(newState) {
	if ('dataOuterRadius' in newState && !this._updatingReadonlyProperty) throw new Error("<Slice>: Cannot set read-only property 'dataOuterRadius'");
	if ('levelsAnnotated' in newState && !this._updatingReadonlyProperty) throw new Error("<Slice>: Cannot set read-only property 'levelsAnnotated'");
};

Slice.prototype._recompute = function _recompute(changed, state) {
	if (changed.outerRadius || changed.labelBandHeight) {
		if (this._differs(state.dataOuterRadius, (state.dataOuterRadius = dataOuterRadius(state)))) changed.dataOuterRadius = true;
	}

	if (changed.levels || changed.innerRadius || changed.dataOuterRadius) {
		if (this._differs(state.levelsAnnotated, (state.levelsAnnotated = levelsAnnotated(state)))) changed.levelsAnnotated = true;
	}
};

/* src/Radar.html generated by Svelte v2.7.0 */

function disciplinesAnnotated({disciplines}) {
  let disciplinesAnnotated = disciplines;
  let practiceCount = 0;
  for (let discipline of disciplines) {
    for (let practice of discipline.practices) {
      practiceCount++;
    }
  }

  let sliceWidth = Math.PI * 2 / practiceCount;

  let currentAngle = 0;
  for (let discipline of disciplines) {
    discipline.startRadians = currentAngle;
    for (let practice of discipline.practices) {
      practice.startRadians = currentAngle;
      currentAngle += sliceWidth;
      practice.endRadians = currentAngle;
    }
    discipline.endRadians = currentAngle;
  }

  return disciplinesAnnotated
}

function data$2() {
  return {
    practiceStroke: "#999999",
    disciplineStroke: "#555555",
    strokeWidth: 0.15,
    baseColor: "#2E8468",
    fontColor: "#22644E",
    diciplineFontColor: "#184738",
    innerRadius: 10,
    outerRadius: 49,

    label: "Label",
    labelBandHeight: 5,
    disciplineBandHeight: 7,
    disciplines: [
      {
        discipline: "Artisanship",
        practices: [
          {practice: "Yellow belt", description: "100% of group members... Yellow Belt training", levels: [
            {portion: 4},
            {portion: 2},
            {portion: 2},
            {portion: 1},
            {portion: 1},
          ]},
          {practice: "Green belt", description: "At least one Green Belt...", levels: [
            {portion: 0},
            {portion: 0},
            {portion: 0},
            {portion: 2},
            {portion: 7},
          ]},
        ]
      },
      {
        discipline: "Architecture & Design",
        practices: [
          {practice: "Threat modeling", description: "blah blah", levels: [
            {portion: 4},
            {portion: 2},
            {portion: 2},
            {portion: 1},
            {portion: 1},
          ]},
        ]
      },
      {
        discipline: "DevSecOps Tools",
        practices: [
          {practice: "PCA in pipeline", description: "blah, blah", levels: [
            {portion: 2},
            {portion: 2},
            {portion: 4},
            {portion: 1},
            {portion: 1},
          ]},
          {practice: "SCA in pipeline", description: "blah, blah", levels: [
            {portion: 7},
            {portion: 0},
            {portion: 1},
            {portion: 1},
            {portion: 1},
          ]},
          {practice: "Working agreements", description: "blah, blah", levels: [
            {portion: 2},
            {portion: 2},
            {portion: 1},
            {portion: 3},
            {portion: 0},
          ]},
        ]
      },

    ]
  }
}
const file$2 = "src/Radar.html";

function create_main_fragment$2(component, ctx) {
	var svg;

	var each_value = ctx.disciplinesAnnotated;

	var each_blocks = [];

	for (var i = 0; i < each_value.length; i += 1) {
		each_blocks[i] = create_each_block$1(component, get_each_context$1(ctx, each_value, i));
	}

	return {
		c: function create() {
			svg = createSvgElement("svg");

			for (var i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}
			setAttribute(svg, "version", "1.1");
			setAttribute(svg, "viewBox", "0 0 100 100");
			setAttribute(svg, "preserveAspectRatio", "xMinYMin meet");
			addLoc(svg, file$2, 0, 0, 0);
		},

		m: function mount(target, anchor) {
			insertNode(svg, target, anchor);

			for (var i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].m(svg, null);
			}
		},

		p: function update(changed, ctx) {
			if (changed.disciplinesAnnotated || changed.outerRadius || changed.disciplineBandHeight || changed.strokeWidth || changed.disciplineStroke || changed.diciplineFontColor || changed.innerRadius) {
				each_value = ctx.disciplinesAnnotated;

				for (var i = 0; i < each_value.length; i += 1) {
					const child_ctx = get_each_context$1(ctx, each_value, i);

					if (each_blocks[i]) {
						each_blocks[i].p(changed, child_ctx);
					} else {
						each_blocks[i] = create_each_block$1(component, child_ctx);
						each_blocks[i].c();
						each_blocks[i].m(svg, null);
					}
				}

				for (; i < each_blocks.length; i += 1) {
					each_blocks[i].d(1);
				}
				each_blocks.length = each_value.length;
			}
		},

		d: function destroy$$1(detach) {
			if (detach) {
				detachNode(svg);
			}

			destroyEach(each_blocks, detach);
		}
	};
}

// (2:2) {#each disciplinesAnnotated as discipline}
function create_each_block$1(component, ctx) {
	var each_anchor;

	var each_value_1 = ctx.discipline.practices;

	var each_blocks = [];

	for (var i = 0; i < each_value_1.length; i += 1) {
		each_blocks[i] = create_each_block_1(component, get_each_context_1(ctx, each_value_1, i));
	}

	var arc_initial_data = {
	 	startRadians: ctx.discipline.startRadians,
	 	endRadians: ctx.discipline.endRadians,
	 	innerRadius: ctx.outerRadius-ctx.disciplineBandHeight,
	 	outerRadius: ctx.outerRadius,
	 	fill: "none",
	 	strokeWidth: ctx.strokeWidth*2,
	 	stroke: ctx.disciplineStroke,
	 	label: ctx.discipline.discipline,
	 	fontColor: ctx.diciplineFontColor
	 };
	var arc = new Arc({
		root: component.root,
		data: arc_initial_data
	});

	var arc_1_initial_data = {
	 	startRadians: ctx.discipline.startRadians,
	 	endRadians: ctx.discipline.endRadians,
	 	outerRadius: ctx.outerRadius,
	 	innerRadius: ctx.innerRadius,
	 	fill: "none",
	 	strokeWidth: ctx.strokeWidth*2,
	 	stroke: ctx.disciplineStroke
	 };
	var arc_1 = new Arc({
		root: component.root,
		data: arc_1_initial_data
	});

	return {
		c: function create() {
			for (var i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].c();
			}

			each_anchor = createComment();
			arc._fragment.c();
			arc_1._fragment.c();
		},

		m: function mount(target, anchor) {
			for (var i = 0; i < each_blocks.length; i += 1) {
				each_blocks[i].m(target, anchor);
			}

			insertNode(each_anchor, target, anchor);
			arc._mount(target, anchor);
			arc_1._mount(target, anchor);
		},

		p: function update(changed, ctx) {
			if (changed.disciplinesAnnotated || changed.outerRadius || changed.disciplineBandHeight) {
				each_value_1 = ctx.discipline.practices;

				for (var i = 0; i < each_value_1.length; i += 1) {
					const child_ctx = get_each_context_1(ctx, each_value_1, i);

					if (each_blocks[i]) {
						each_blocks[i].p(changed, child_ctx);
					} else {
						each_blocks[i] = create_each_block_1(component, child_ctx);
						each_blocks[i].c();
						each_blocks[i].m(each_anchor.parentNode, each_anchor);
					}
				}

				for (; i < each_blocks.length; i += 1) {
					each_blocks[i].d(1);
				}
				each_blocks.length = each_value_1.length;
			}

			var arc_changes = {};
			if (changed.disciplinesAnnotated) arc_changes.startRadians = ctx.discipline.startRadians;
			if (changed.disciplinesAnnotated) arc_changes.endRadians = ctx.discipline.endRadians;
			if (changed.outerRadius || changed.disciplineBandHeight) arc_changes.innerRadius = ctx.outerRadius-ctx.disciplineBandHeight;
			if (changed.outerRadius) arc_changes.outerRadius = ctx.outerRadius;
			if (changed.strokeWidth) arc_changes.strokeWidth = ctx.strokeWidth*2;
			if (changed.disciplineStroke) arc_changes.stroke = ctx.disciplineStroke;
			if (changed.disciplinesAnnotated) arc_changes.label = ctx.discipline.discipline;
			if (changed.diciplineFontColor) arc_changes.fontColor = ctx.diciplineFontColor;
			arc._set(arc_changes);

			var arc_1_changes = {};
			if (changed.disciplinesAnnotated) arc_1_changes.startRadians = ctx.discipline.startRadians;
			if (changed.disciplinesAnnotated) arc_1_changes.endRadians = ctx.discipline.endRadians;
			if (changed.outerRadius) arc_1_changes.outerRadius = ctx.outerRadius;
			if (changed.innerRadius) arc_1_changes.innerRadius = ctx.innerRadius;
			if (changed.strokeWidth) arc_1_changes.strokeWidth = ctx.strokeWidth*2;
			if (changed.disciplineStroke) arc_1_changes.stroke = ctx.disciplineStroke;
			arc_1._set(arc_1_changes);
		},

		d: function destroy$$1(detach) {
			destroyEach(each_blocks, detach);

			if (detach) {
				detachNode(each_anchor);
			}

			arc.destroy(detach);
			arc_1.destroy(detach);
		}
	};
}

// (3:4) {#each discipline.practices as practice}
function create_each_block_1(component, ctx) {

	var slice_initial_data = {
	 	startRadians: ctx.practice.startRadians,
	 	endRadians: ctx.practice.endRadians,
	 	outerRadius: ctx.outerRadius-ctx.disciplineBandHeight,
	 	levels: ctx.practice.levels,
	 	label: ctx.practice.practice
	 };
	var slice = new Slice({
		root: component.root,
		data: slice_initial_data
	});

	return {
		c: function create() {
			slice._fragment.c();
		},

		m: function mount(target, anchor) {
			slice._mount(target, anchor);
		},

		p: function update(changed, ctx) {
			var slice_changes = {};
			if (changed.disciplinesAnnotated) slice_changes.startRadians = ctx.practice.startRadians;
			if (changed.disciplinesAnnotated) slice_changes.endRadians = ctx.practice.endRadians;
			if (changed.outerRadius || changed.disciplineBandHeight) slice_changes.outerRadius = ctx.outerRadius-ctx.disciplineBandHeight;
			if (changed.disciplinesAnnotated) slice_changes.levels = ctx.practice.levels;
			if (changed.disciplinesAnnotated) slice_changes.label = ctx.practice.practice;
			slice._set(slice_changes);
		},

		d: function destroy$$1(detach) {
			slice.destroy(detach);
		}
	};
}

function get_each_context$1(ctx, list, i) {
	const child_ctx = Object.create(ctx);
	child_ctx.discipline = list[i];
	child_ctx.each_value = list;
	child_ctx.discipline_index = i;
	return child_ctx;
}

function get_each_context_1(ctx, list, i) {
	const child_ctx = Object.create(ctx);
	child_ctx.practice = list[i];
	child_ctx.each_value_1 = list;
	child_ctx.practice_index = i;
	return child_ctx;
}

function Radar(options) {
	this._debugName = '<Radar>';
	if (!options || (!options.target && !options.root)) throw new Error("'target' is a required option");
	init(this, options);
	this._state = assign(data$2(), options.data);
	this._recompute({ disciplines: 1 }, this._state);
	if (!('disciplines' in this._state)) console.warn("<Radar> was created without expected data property 'disciplines'");

	if (!('outerRadius' in this._state)) console.warn("<Radar> was created without expected data property 'outerRadius'");
	if (!('disciplineBandHeight' in this._state)) console.warn("<Radar> was created without expected data property 'disciplineBandHeight'");
	if (!('strokeWidth' in this._state)) console.warn("<Radar> was created without expected data property 'strokeWidth'");
	if (!('disciplineStroke' in this._state)) console.warn("<Radar> was created without expected data property 'disciplineStroke'");
	if (!('diciplineFontColor' in this._state)) console.warn("<Radar> was created without expected data property 'diciplineFontColor'");
	if (!('innerRadius' in this._state)) console.warn("<Radar> was created without expected data property 'innerRadius'");
	this._intro = true;

	if (!options.root) {
		this._oncreate = [];
		this._beforecreate = [];
		this._aftercreate = [];
	}

	this._fragment = create_main_fragment$2(this, this._state);

	if (options.target) {
		if (options.hydrate) throw new Error("options.hydrate only works if the component was compiled with the `hydratable: true` option");
		this._fragment.c();
		this._mount(options.target, options.anchor);

		this._lock = true;
		callAll(this._beforecreate);
		callAll(this._oncreate);
		callAll(this._aftercreate);
		this._lock = false;
	}
}

assign(Radar.prototype, protoDev);

Radar.prototype._checkReadOnly = function _checkReadOnly(newState) {
	if ('disciplinesAnnotated' in newState && !this._updatingReadonlyProperty) throw new Error("<Radar>: Cannot set read-only property 'disciplinesAnnotated'");
};

Radar.prototype._recompute = function _recompute(changed, state) {
	if (changed.disciplines) {
		if (this._differs(state.disciplinesAnnotated, (state.disciplinesAnnotated = disciplinesAnnotated(state)))) changed.disciplinesAnnotated = true;
	}
};

var app = new Radar({
	target: document.body,
	data: {
		Slices: [
			{startAngle: 0, endAngle: Math.Pi / 4}
		]
	}
});

return app;

}());
//# sourceMappingURL=bundle.js.map
